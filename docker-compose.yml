services:
  il-news-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - huggingface:/home/nonroot/.cache/huggingface/
    restart: unless-stopped

volumes:
  huggingface: {}
